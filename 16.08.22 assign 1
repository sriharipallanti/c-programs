#include <stdio.h>

int main(void)
{
    int a[4],i,count=0;
    for(i=0;i<4;i++)
    scanf("%d",&a[i]);
    for(i=0;i<4;i++)
    {
        if(a[i]>=10&&a[i]<=100)
        {
            count=count+1;
        }
    }
    printf("%d",count);
	return 0;
}
